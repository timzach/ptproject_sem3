import java.util.List;
import org.


public class Prim {

    private List<Vertex> graph;

    public void run() {
        if (graph.size() > 0) {
            graph.get(0).setVisited(true);
        }
        while (isDisconnected()){
            Edge
        }
    }
}
